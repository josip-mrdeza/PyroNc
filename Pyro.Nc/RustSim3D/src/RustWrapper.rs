use crate::Vector3::Vector3;

pub struct RustWrapper
{
    pub position: Vector3,
    pub trV: Vector3,
    pub vert: Vector3,
    pub realVert: Vector3,
    pub radius: f32,
    pub vertices: Vec<Vector3>
}

impl RustWrapper{
    
}